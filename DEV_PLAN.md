 # XpertEval - 项目开发计划 (DEV_PLAN.md)

## 1. 概述

本文档详细规划了 XpertEval 项目的开发步骤、各阶段目标及关键注意事项。开发过程将遵循模块化、迭代式的原则，优先实现核心功能，逐步完善并扩展。

**核心原则**：
- **中文优先**：所有代码注释、文档、UI文本均使用中文。
- **英文命名**：文件、目录、类、函数、变量等标识符使用英文。
- **持续集成与测试**：尽早引入并坚持单元测试和集成测试。
- **文档同步**：开发与文档编写同步进行。

## 2. 开发阶段与任务分解

### 阶段一：核心框架搭建 (预计1-2周)

**目标**：搭建项目基础结构，实现模型配置加载和基本的API调用能力。

1.  **任务1.1：项目初始化与环境配置**
    -   **内容**：
        -   创建 `STRUCTURE.md` 中定义的基础目录结构。
        -   初始化 Git 仓库，配置 `.gitignore` 文件。
        -   创建 `VERSION` 文件并设置初始版本号 (如 `0.1.0-alpha`)。
        -   创建 `requirements.txt` 并列出初期核心依赖 (如 `PyYAML`, `requests`, `gradio`)。
        -   创建 `LICENSE` 文件 (Apache 2.0)。
    -   **注意事项**：
        -   确保目录结构与 `STRUCTURE.md` 一致。
        -   `.gitignore` 需覆盖 Python 临时文件、IDE配置、测试覆盖率报告、本地数据和结果等。

2.  **任务1.2：模型配置模块 (`xperteval/config/`)**
    -   **内容**：
        -   实现 `config_loader.py`。
        -   支持从 YAML (首选) 和 JSON 文件加载模型配置。
        -   解析 `OPENAI_API_BASE`, `OPENAI_API_KEY`, `MODEL_NAME`, `MODEL_TYPE`, `MAIN_API`。
        -   实现全局默认参数（`MAX_TOKENS`, `TEMPERATURE` 等）的加载与合并逻辑。
        -   实现配置校验逻辑：至少两个有效模型配置；`MAIN_API` 的正确设置（唯一true或默认第一个）。
    -   **注意事项**：
        -   配置加载失败时应有清晰的错误提示。
        -   提供详细的中文注释说明配置项的含义和格式要求。
        -   编写单元测试 (`tests/unit/test_config_loader.py`)。

3.  **任务1.3：API 调用模块 (`xperteval/core/api_caller.py`)**
    -   **内容**：
        -   实现一个函数或类，用于向 OpenAI API 兼容接口发送请求。
        -   支持 HTTPS POST 请求，包含必要的头部信息 (如 `Authorization: Bearer <API_KEY>`)。
        -   根据模型配置中的参数构造请求体。
        -   处理 API 响应：解析 JSON 结果，提取模型输出。
        -   基本的错误处理：网络错误、API 返回错误码等。
        -   支持请求超时设置 (`REQUEST_TIMEOUT`)。
    -   **注意事项**：
        -   API Key 等敏感信息不应硬编码或出现在日志中（除非是调试模式下的明确指示）。
        -   考虑 API 速率限制的应对（初期可暂不实现复杂重试，但需预留接口）。
        -   对不同 `MODEL_TYPE`（text, vision, audio, mllm）可能需要的不同请求结构做初步考虑，初期可先重点实现文本和mllm。
        -   编写单元测试（可 mock掉实际的 http 请求）。

### 阶段二：自动化评测流程 (预计2-3周)

**目标**：实现基于配置和数据集的自动化评测核心逻辑。

1.  **任务2.1：数据集处理模块 (`xperteval/datasets/`)**
    -   **内容**：
        -   定义 `base_dataset.py`，包含数据集加载、迭代样本等抽象方法。
        -   实现 `ms_swift_parser.py`，用于解析 `ms-swift` 标准格式的数据集。
            -   支持单轮问答 (`query`, `response`)。
            -   支持多轮问答 (`history`, `query`, `response`)。
            -   处理多模态数据引用（如图片路径、音频文件路径）。
        -   实现 `registered_datasets.py` (可选，初期可直接指定路径) 来管理内置数据集。
    -   **注意事项**：
        -   清晰定义数据样本的内部表示格式（如字典或数据类）。
        -   确保能正确处理不同模态数据的占位符或实际内容路径。
        -   支持从本地文件系统加载数据集。
        -   编写单元测试。

2.  **任务2.2：评测器基类与初步实现 (`xperteval/core/base_evaluator.py`, `xperteval/evaluators/`)**
    -   **内容**：
        -   创建 `base_evaluator.py`，定义评测器的通用接口 (例如 `evaluate(predictions, references)` 方法，返回评测分数和详细结果)。
        -   在 `evaluators/common/` 下实现1-2个简单的通用评测指标 (例如，针对分类任务的准确率 `accuracy.py`，或针对文本生成的简单匹配度)。
    -   **注意事项**：
        -   评测指标的输入应明确（模型输出列表，参考答案列表）。
        -   评测结果应包含数值分数和可能的解释性信息。
        -   设计时考虑未来扩展更多复杂评测指标。

3.  **任务2.3：自动化评测引擎 (`xperteval/core/auto_eval_engine.py`)**
    -   **内容**：
        -   接收模型配置列表、数据集实例、评测器列表作为输入。
        -   遍历数据集中的每个样本。
        -   对于每个样本，调用配置中的所有模型（通过 `api_caller.py`）获取预测结果。
        -   将预测结果和参考答案传递给指定的评测器进行评估。
        -   汇总所有样本的评测结果。
    -   **注意事项**：
        -   注意处理模型API调用失败的情况，允许部分失败不中断整个评测流程，但需记录。
        -   考虑评测过程中的日志记录，方便追踪进度和调试。
        -   结果汇总时，要能区分不同模型的表现。

### 阶段三：报告生成与命令行工具 (预计1-2周)

**目标**：能够将评测结果生成结构化报告，并提供命令行工具运行评测。

1.  **任务3.1：报告生成模块 (`xperteval/reporters/report_generator.py`)**
    -   **内容**：
        -   接收自动化评测引擎输出的汇总结果。
        -   将结果格式化为易读的报告（初期可以是 Markdown 或 JSON 格式）。
        -   报告应包含：
            -   评测概述（参与评测的模型、数据集、时间等）。
            -   各模型在各评测指标上的得分对比。
            -   以主API (`MAIN_API=true`) 为基准的对比分析。
    -   **注意事项**：
        -   报告结构清晰，易于理解。
        -   为后续生成 HTML 报告或图表预留数据结构。

2.  **任务3.2：命令行入口 (`main.py`)**
    -   **内容**：
        -   使用 `argparse` 或类似库解析命令行参数。
        -   必需参数：模型配置文件路径。
        -   可选参数：数据集名称/路径、输出目录、指定评测指标等。
        -   调用配置加载、数据集加载、评测引擎、报告生成等模块，完成端到端的评测流程。
        -   将生成的报告保存到文件或打印到控制台。
    -   **注意事项**：
        -   提供清晰的命令行帮助信息 (`--help`)。
        -   错误处理和用户反馈要友好。

### 阶段四：人工评测功能 (预计2-3周)

**目标**：实现人工评测的核心逻辑和数据流。

1.  **任务4.1：人工评测引擎 (`xperteval/core/manual_eval_engine.py`)**
    -   **内容**：
        -   支持从数据集加载问题，或接收即时输入的问题。
        -   为每个问题，调用所有已配置模型的 API 获取答案，确保答案是匿名的（不展示模型来源给评分人）。
        -   管理问题的分发和答案的收集（初期可为单用户同步操作）。
        -   定义人工评分的维度（如1-10分，可配置多个维度）。
        -   校验人工评测的有效性（如至少20题，题目类型多样性 - 初期可简化为数量校验）。
    -   **注意事项**：
        -   答案匿名化是关键。
        -   考虑如何存储人工评分结果（初期可为内存或简单文件）。

2.  **任务4.2：人工评测结果整合**
    -   **内容**：
        -   将收集到的人工评分结果进行汇总和平均。
        -   将人工评测结果也纳入 `report_generator.py`，与自动化评测结果一同展示或作为独立部分展示。
    -   **注意事项**：
        -   明确人工评分结果的统计和展示方式。

### 阶段五：WebUI 开发 (`gradio`) (预计3-4周)

**目标**：提供一个用户友好的Gradio Web界面，集成核心评测功能。

1.  **任务5.1：Gradio 基础界面 (`xperteval/webui/app_gradio.py`, `app.py`)**
    -   **内容**：
        -   创建 `app.py` 作为 Gradio 应用启动脚本。
        -   在 `app_gradio.py` 中设计基本布局。
        -   实现模型API配置界面：允许用户通过表单增删改查模型配置（可考虑导入导出YAML/JSON）。
        -   实现数据集选择界面：列出已集成的评测集，允许用户上传自定义数据集（需符合`ms-swift`格式）。
    -   **注意事项**：
        -   界面操作应尽可能直观易懂。
        -   配置的持久化（例如，保存到服务器端的 `config.yaml` 或浏览器本地存储，初期可简化）。

2.  **任务5.2：集成自动化评测到 WebUI**
    -   **内容**：
        -   添加“开始评测”按钮，触发后端 `auto_eval_engine.py`。
        -   实时显示评测进度（如当前处理的样本，已完成百分比）。
        -   评测结束后，在界面上展示生成的评测报告（可内嵌Markdown或链接到HTML报告）。
    -   **注意事项**：
        -   长时任务（评测过程）应有明确的进度反馈，避免界面卡死。
        -   Gradio 的状态管理和回调函数设计。

3.  **任务5.3：集成人工评测到 WebUI**
    -   **内容**：
        -   人工评测界面：
            -   展示问题（来自数据集或用户输入）。
            -   并排或分页展示各模型匿名生成的答案。
            -   提供打分控件（如滑块或输入框）供用户对每个答案按维度评分。
            -   “下一题”、“提交评分”等操作按钮。
        -   连接后端 `manual_eval_engine.py`。
    -   **注意事项**：
        -   保证模型答案的匿名性。
        -   用户评分体验要流畅。

### 阶段六：中医药领域特定功能 (穿插进行/后续重点) (预计2-3周)

**目标**：增强框架对中医药领域评测的支持。

1.  **任务6.1：集成中医药评测数据集 (`data/integrated/tcm_*`)**
    -   **内容**：
        -   收集或整理中医药相关的评测数据（面诊、舌诊图片，问诊对话，医案等）。
        -   将其转换为 `ms-swift` 兼容格式，并集成到项目中。
        -   在 `docs/` 中添加对这些数据集的说明。
    -   **注意事项**：
        -   数据质量和标注的准确性是关键。
        -   涉及隐私数据需脱敏处理。

2.  **任务6.2：开发中医药特定评测指标 (`xperteval/evaluators/tcm/`)**
    -   **内容**：
        -   针对中医四诊、辨证、方剂推荐等，设计并实现具体的评测指标。
        -   例如：舌质舌苔识别准确率、证候与标准答案的一致性、推荐方剂与经典方剂的匹配度等。
    -   **注意事项**：
        -   部分指标可能需要引入中医知识库或专家经验作为参考。
        -   这些指标的实现可能较复杂，需要领域知识。

### 阶段七：文档、测试、部署与完善 (持续进行/收尾阶段) (预计2-3周)

**目标**：确保项目质量，提供完善的文档和便捷的部署方式。

1.  **任务7.1：完善项目文档 (`docs/`)**
    -   **内容**：
        -   撰写详细的用户手册（安装、配置、使用WebUI、CLI）。
        -   撰写开发者指南（项目架构、如何扩展评测指标、如何添加数据集）。
        -   配置 GitHub Pages (`.github/workflows/docs_deploy.yml`) 实现文档自动部署。
        -   尝试实现 Ant Design 风格的文档页面 (可能需要 Jekyll 主题或自定义 CSS/JS)。
    -   **注意事项**：
        -   文档内容准确、清晰、易懂。
        -   确保所有公开接口和配置项都有文档说明。

2.  **任务7.2：强化测试 (`tests/`)**
    -   **内容**：
        -   提高单元测试覆盖率。
        -   编写更多集成测试用例，覆盖主要的用户场景和评测流程。
        -   考虑加入性能测试（如适用）。
    -   **注意事项**：
        -   测试应自动化，并易于执行 (`scripts/run_tests.sh`)。

3.  **任务7.3：Docker 化部署 (`Dockerfile`, `scripts/build_docker.sh`)**
    -   **内容**：
        -   编写 `Dockerfile`，定义项目运行环境和启动命令。
        -   提供 `build_docker.sh` 脚本方便用户构建镜像。
        -   测试 Docker 镜像的可用性。
    -   **注意事项**：
        -   Docker 镜像应尽可能小，并包含所有运行时依赖。
        -   确保容器内数据持久化方案（如挂载卷）的说明。

4.  **任务7.4：性能评测指标的实现**
    -   **内容**：
        -   在 `api_caller.py` 或评测引擎中记录模型响应时间。
        -   计算平均响应速度、Token使用效率等指标。
        -   将性能指标也纳入评测报告。
    -   **注意事项**：
        -   性能指标的统计方式要明确。

5.  **任务7.5：代码审查与重构**
    -   **内容**：
        -   对整体代码进行审查，优化代码结构，提高可读性和可维护性。
        -   确保所有代码均有必要的中文注释。
        -   统一代码风格。

## 3. 持续进行的事项

-   **错误处理与日志记录**：在各模块中加入健壮的错误处理和详细的日志记录 (`xperteval/utils/log_utils.py`)。
-   **依赖管理**：定期更新 `requirements.txt`。
-   **版本控制**：遵循 Git 工作流，频繁提交，有意义的提交信息，使用标签发布版本。
-   **社区反馈与迭代**：项目发布后，根据用户反馈持续改进和迭代。

本开发计划提供了一个大致的路线图，实际执行中可能会根据具体情况调整优先级和时间安排。