# XpertEval - 项目需求文档

## 1. 引言

### 1.1 项目名称
XpertEval (全模态大模型一站式评测框架)

### 1.2 项目目标
本项目旨在构建一个通用的、轻量级的全模态大模型一站式评测框架。该框架不仅支持对通用领域大模型能力的评测，还将重点集成对专业领域（初期聚焦于中医药领域）全模态大模型的评测能力。核心目标是为微调后的中医药多诊合参全模态大模型提供全面的效果评估，既要验证其在特定领域（如中医四诊、辨证论治、方剂推荐等）的能力提升，也要确保其通用能力（如常识问答、多模态理解等）得到保留。

### 1.3 项目背景
随着大模型技术的飞速发展，基于开源全模态基座模型进行特定领域（如中医药）的微调已成为趋势。然而，如何科学、高效地评测这些微调后模型的综合能力，成为了一个亟待解决的问题。现有的评测工具往往或侧重通用能力，或缺乏对特定专业领域的深度支持，且部署复杂。XpertEval 旨在解决以下痛点：
- **领域定制与通用性兼顾**：既要评估模型在中医药领域的专业表现，也要监控其通用能力的稳定性。
- **轻量化与易用性**：避免复杂的本地模型部署依赖，100% 基于 OpenAI API 兼容接口进行评测，简化用户操作。
- **多模型对比分析**：支持至少两个模型同时进行评测，提供横向对比分析。
- **评测模式多样性**：同时支持自动化评测和人工辅助评测，满足不同评测需求。

## 2. 核心功能

### 2.1 评测方式

#### 2.1.1 API 兼容性
- 所有大模型的评测均通过调用 OpenAI API 兼容的接口完成。
- 框架负责基于用户提供的评测数据调用大模型接口，获取模型响应，并进行后续分析。
- 框架本身不包含任何本地部署大模型的代码，保持轻量级。

#### 2.1.2 模型配置
- 用户需提供待评测大模型的配置信息，支持 YAML 或 JSON 格式进行配置。
- 每个模型的配置信息包括：
    - `OPENAI_API_BASE`: API 服务地址 (例如: `https://api.openai.com/v1`)
    - `OPENAI_API_KEY`: API 密钥
    - `MODEL_NAME`: 模型名称
    - `MODEL_TYPE`: 模型类型，枚举值：`<text|vision|audio|mllm>` (文本，视觉，音频，多模态语言模型)
    - `MAIN_API`: 是否为主评测 API，布尔值 (`true`|`false`)
    - `MAX_TOKENS`: 最大生成 token 数 (可选，默认为 4096)
    - `TEMPERATURE`: 温度参数 (可选，默认为 0.7)
    - `TOP_P`: Top-P 采样参数 (可选，默认为 0.95)
    - `TOP_K`: Top-K 采样参数 (可选，默认为 0.95)
    - `FREQUENCY_PENALTY`: 频率惩罚 (可选，默认为 0.0)
    - `PRESENCE_PENALTY`: 存在惩罚 (可选，默认为 0.0)
    - `REQUEST_TIMEOUT`: 请求超时时间 (秒) (可选，默认为 120)

#### 2.1.3 全局默认参数
- 可为模型请求参数设置全局默认值。若单个模型配置中未提供特定参数或留空，则使用全局默认值。
- 全局默认参数包括：
    - `MAX_TOKENS=4096`
    - `TEMPERATURE=0.7`
    - `TOP_P=0.95`
    - `TOP_K=0.95`
    - `FREQUENCY_PENALTY=0.0`
    - `PRESENCE_PENALTY=0.0`
    - `REQUEST_TIMEOUT=120`

#### 2.1.4 多模型对比
- 每次评测至少需要提供两组有效且能成功连接的大模型 API 配置。
- 用户需在配置中指定一个模型为"主评测 API" (`MAIN_API=true`)，其他模型为对比 API (`MAIN_API=false`)。
    - 若设置多个 `MAIN_API=true`，则以配置文件中出现的第一个为准，其余视为 `false`。
    - 若未设置任何 `MAIN_API=true`，则默认以配置文件中的第一个模型为主评测 API。
- 若有效可测通的大模型数量不足两个，评测流程将报错并终止。

#### 2.1.5 人工评测模式
- **基于评测集的人工评测**：
    - 系统自动从指定的评测数据集中抽取题目。
    - 调用所有已配置的大模型接口，为每个模型匿名生成答案。
    - 人工评测员从多个预设或自定义的维度/角度对每个模型的匿名答案进行评分（1-10分制）。
- **用户自定义提问的人工评测**：
    - 用户在界面或通过特定方式输入自定义问题。
    - 系统调用所有已配置的大模型接口，为每个模型匿名生成答案。
    - 人工评测员对匿名答案进行多维度评分（1-10分制）。
- **有效性要求**：
    - 每次完整的人工评测任务至少需要包含 20 道题目。
    - 这 20 道题目应尽可能覆盖多种类型，如常识类、专业类、纯文本、图文、音视频等，以保证评测的全面性。
    - 未达到题目数量要求的人工评测结果不计入最终的评测报告。

### 2.2 评测内容

#### 2.2.1 通用能力评测
- **自我认知**：模型对其自身身份、能力的认知。
- **企业/单位认知**：模型对特定组织机构信息的了解程度（如适用）。
- **常识问答**：对日常生活、科学、文化等领域常识的掌握。
- **图像描述**：理解并用自然语言描述图片内容的能力。
- **声音识别**：识别和理解音频内容的能力（如语音转文字、特定声音事件识别）。
- **文本摘要**：从长文本中提取核心内容，生成简明摘要的能力。
- **实体抽取**：从文本中识别并抽取出命名实体（如人名、地名、组织机构名等）。
- **关系抽取**：识别文本中实体之间的语义关系。
- 其他基础大模型应具备的通用能力层面。

#### 2.2.2 中医药专业能力评测 (初期重点)
- **中医四诊信息判定**：
    - **望诊**：面象、舌象（舌质、舌苔）的识别与分类。
    - **闻诊**：声音（患者语音、咳嗽音、呼吸音等）的特征分析与判定。
    - **问诊**：基于症状描述的理解与关键信息提取（需支持多轮问答）。
    - **切诊**：脉象数据或图片的分析与判定。
- **中医辨证论治**：
    - 基于四诊信息综合分析，进行中医证候诊断。
    - 进行中西医疾病的初步诊断或辅助诊断。
- **方剂推荐**：
    - 根据辨证及诊断结果，推荐相关中药方剂。
    - 期望提供至少 3 组按优先级排序的方剂方案。
    - 每组方案应包括：方剂名称（若有）、中药组成（药材名及对应剂量）。
    - 提供方剂推荐的解释（为何此方可能有效）。
    - 标注方剂来源的准确性。
- **其他中医疗法推荐**：
    - 根据辨证及诊断结果，推荐针灸、推拿等其他适宜的中医治疗方法。
    - 提供治疗方法推荐的解释（为何此疗法可能有效）。
    - 标注疗法依据来源的准确性。

#### 2.2.3 性能评测
- **响应速度**：模型生成答案的平均耗时。
- **吞吐量**：在一定时间内模型能处理的请求数量（如适用）。
- **Token 使用效率**：生成有效答案所消耗的 Token 数量。

#### 2.2.4 评测报告
- 评测结束后，系统应自动生成一份详细的评测报告。
- 报告需从多个维度对模型能力进行可视化展示（如图表、雷达图等）。
- 报告内容应以用户指定的主评测 API 为中心，进行对比分析和总结。

### 2.3 评测数据

#### 2.3.1 数据来源
- **集成开源评测集**：逐步集成社区中知名的、常用的针对文本、图像、音频、视频等模态的开源评测数据集。
- **自建中医药评测集**：整理并构建一部分与中医药相关的多模态评测数据集，包括但不限于：
    - 面诊图片库（带标签）
    - 舌诊图片库（带标签）
    - 脉象数据/图片（带标签）
    - 问诊对话数据（带标签）
    - 中医医案数据（带标签）
- **用户自定义评测集**：支持用户按照统一的数据格式上传并使用自己的评测数据。

#### 2.3.2 数据格式
- 统一数据格式，参考并兼容开源模型训练框架 `ms-swift` 的标准数据集格式。
- 具体格式细节可参考：[ms-swift 自定义数据集文档](https://swift.readthedocs.io/zh-cn/latest/Customization/%E8%87%AA%E5%AE%9A%E4%B9%89%E6%95%B0%E6%8D%AE%E9%9B%86.html#id2)
- 需明确支持单轮及多轮问答数据的评测。

## 3. 技术要求

### 3.1 文档
- 项目需提供完善的开发文档和用户手册。
- 文档托管于 GitHub，并能够通过 GitHub Pages 进行在线部署和访问。
- 文档页面的整体风格应采用 Ant Design 组件库的视觉风格，以保证专业性和一致性。

### 3.2 WebUI
- 使用 Gradio 框架开发一个用户友好的 Web 界面。
- WebUI 功能包括：
    - **免代码 API 配置**：允许用户通过界面方便地添加、编辑和管理待评测大模型的 API 配置。
    - **数据集选择**：允许用户选择使用集成的评测数据集或上传自定义数据集。
    - **评测启动与监控**：一键启动评测任务，并实时显示评测进度。
    - **评测报告查阅**：在线展示和下载生成的评测报告。
    - **人工评测集成**：集成前述人工评测功能，包括题目展示、答案录入、多维度打分等。

### 3.3 开发语言与架构
- **主要开发语言**：Python。
- **架构设计**：采用模块化设计，确保代码结构清晰、易于维护。
- **扩展性**：支持插件式扩展，方便未来添加新的评测模态、评测指标或数据集支持。

### 3.4 部署与版本管理
- **代码托管**：项目代码统一托管在 GitHub。
- **Docker 化部署**：提供 Dockerfile，支持通过 Docker 进行一键部署。
- **版本控制**：通过项目根目录下的 `VERSION` 文件来管理和声明项目版本号。
- **相关配置文件**：项目应包含必要的配置文件，如 `.gitignore`, `requirements.txt` 等。

### 3.5 开发过程管理
- 项目开发过程中，需创建并维护一份详细的开发记录文档 (`DEV_RECORDS.md`)。
- 该文档需模块化拆解项目任务，明确各模块功能点，并实时追踪各任务的执行状态（待执行、执行中、已完成及完成时间）。
- 开发人员（或AI助手）在完成每个主要开发步骤后，应重新审阅项目需求文档 (`REQUIREMENTS.md`)、架构设计文档 (`STRUCTURE.md`)、开发计划文档 (`DEV_PLAN.md`) 及开发记录文档 (`DEV_RECORDS.md`)，确保开发方向与总体目标一致。

## 4. 非功能性需求
- **轻量级**：核心框架避免不必要的依赖，特别是大型模型库的本地依赖。
- **易用性**：无论是开发者还是最终用户，都能方便地部署、配置和使用本框架。
- **可维护性**：代码注释清晰，模块划分合理，便于后续迭代开发。
- **可扩展性**：方便添加新的评测维度、评测模型类型和评测数据集。
- **中文优先**：项目的主要文档、注释、界面提示等均使用中文，面向中文用户。

## 5. 未来展望
- 持续集成更多优秀的开源评测数据集。
- 扩展对更多专业领域（如法律、金融等）大模型的评测能力。
- 探索更细致、更自动化的评测指标和方法。
- 构建评测结果分享和交流社区。 